# Data

- `raw/data.csv` — labeled country/year records with a `Life expectancy` target and WHO-style health, demographic, and socio-economic features

The notebook splits this file into train / validation / test. No separate unlabeled evaluation set is used.
