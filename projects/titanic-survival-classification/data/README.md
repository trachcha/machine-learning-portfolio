# Data

- `raw/data.csv` — labeled passenger records with a `survived` target and demographic / ticket features

The notebook splits this file into train / validation / test. Source: Titanic-style survival dataset (tabular).
