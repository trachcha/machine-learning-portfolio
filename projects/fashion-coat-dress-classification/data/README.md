# Data

- `raw/train.csv` — labeled 28×28 grayscale images (`label` + 784 pixel columns)
- `raw/evaluate.csv` — unlabeled evaluation set (`ID` + 784 pixel columns)

Source: Fashion-MNIST subset (coat vs dress). Files are included so the project runs out of the box.
